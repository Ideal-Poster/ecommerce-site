type Query {
  products: [Product]
  categories: [Category]
  categoryFilter(id: ID!): [Product]
  brandFilter: [Product]
}

type Product {
  id: ID!
  brand: String
  name: String
  category: String
  color: String
  description: String
  price: Float
  stock: Int
}

type Category {
  id: ID!
  name: String
}